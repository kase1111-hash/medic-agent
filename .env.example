# Medic Agent - Environment Variables
# Phase 7: Deployment & Operations
#
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!

# =============================================================================
# Required Secrets
# =============================================================================

# SIEM API Key for threat intelligence queries
# Get this from your SIEM administrator
# MINIMUM 16 characters, no placeholder values in production
SIEM_API_KEY=your-siem-api-key-here

# Medic Agent API Authentication Keys
# Generate secure keys using: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Admin API Key - Full access to all endpoints
MEDIC_ADMIN_API_KEY=generate-a-secure-random-key-here
#
# Operator API Key - Can approve/deny resurrections and view data
MEDIC_OPERATOR_API_KEY=generate-a-secure-random-key-here
#
# Viewer API Key - Read-only access
MEDIC_VIEWER_API_KEY=generate-a-secure-random-key-here

# Grafana admin password (min 8 characters recommended)
# Used for accessing the Grafana dashboard at http://localhost:3000
GRAFANA_PASSWORD=change-this-password

# =============================================================================
# Optional Configuration
# =============================================================================

# Grafana admin username (default: admin)
# GRAFANA_ADMIN_USER=admin

# Redis password (if Redis authentication is enabled)
# REDIS_PASSWORD=your-redis-password

# =============================================================================
# Development Overrides
# =============================================================================

# For local development with mock SIEM, you can use a placeholder:
# SIEM_API_KEY=dev-api-key-local

# Environment (development | staging | production)
# Controls security enforcement
MEDIC_ENV=development

# Medic Agent mode (observer | manual | semi_auto | full_auto)
# MEDIC_MODE=observer

# Log level (DEBUG | INFO | WARNING | ERROR)
# MEDIC_LOG_LEVEL=DEBUG

# =============================================================================
# Security Notes
# =============================================================================
#
# 1. API Keys:
#    - Use strong random keys (minimum 32 characters)
#    - Never use placeholder values in production
#    - Rotate keys regularly
#    - Store keys in secrets manager for production (not .env)
#
# 2. SIEM API Key:
#    - Must be set via environment variable (not config file)
#    - Minimum 16 characters required
#    - Production enforces strict validation
#
# 3. Environment:
#    - Set MEDIC_ENV=production for production deployments
#    - Production mode enforces:
#      * API authentication required
#      * HTTPS-only CORS origins
#      * Strong API key validation
#      * No placeholder keys allowed
